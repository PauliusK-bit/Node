<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <% if (assignments.length > 0) { %>
    <h1>Assignments List (<%= assignments.length %>)</h1>
    <% } else { %>
    <h1>No assignments yet...</h1>
    <% } %> <% if (newAssignmentButton.url && newAssignmentButton.title) { %>
    <a href="<%= newAssignmentButton.url %>"
      ><%= newAssignmentButton.title %></a
    >
    <% } %> <% if (assignments.length > 0) { %>
    <ul>
      <% assignments.forEach(assignment => { %>
      <li>
        <a href="/assignments/<%= assignment.id %>"><%= assignment.name %></a>
      </li>
      <% }) %>
    </ul>
    <% } %>
  </body>
</html>
